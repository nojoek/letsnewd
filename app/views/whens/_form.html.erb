<%= form_for(@when) do |f| %>
  <% if @when.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@when.errors.count, "error") %> prohibited this when from being saved:</h2>

      <ul>
      <% @when.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time_option %><br />
    <%= f.time_select :time_option %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
